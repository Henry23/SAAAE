<%= form_for @hours_reserved, :html => { :class => "form-horizontal hours_reserved" } do |f| %>

  <% if @hours_reserved.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@hours_reserved.errors.count, "error") %> prohibited this hours_reserved from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @hours_reserved.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :hour, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :hour, :class => 'form-control' %>
    </div>
    <%=f.error_span(:hour) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                hours_reserveds_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
