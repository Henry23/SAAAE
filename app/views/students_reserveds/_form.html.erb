<%= form_for @students_reserved, :html => { :class => "form-horizontal students_reserved" } do |f| %>

  <% if @students_reserved.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@students_reserved.errors.count, "error") %> prohibited this students_reserved from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @students_reserved.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>


  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                students_reserveds_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>

<%=  @reservation.id %>
<%=  @carrels.max_number / 2 %>