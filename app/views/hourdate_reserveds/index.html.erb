<%- model_class = HourdateReserved -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:hora) %></th>
      <th><%= model_class.human_attribute_name(:fecha) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @hourdate_reserveds.each do |hourdate_reserved| %>
      <tr>
        <td><%= link_to hourdate_reserved.id, hourdate_reserved_path(hourdate_reserved) %></td>
        <td><%= hourdate_reserved.hora %></td>
        <td><%= hourdate_reserved.fecha %></td>
        <td><%=l hourdate_reserved.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_hourdate_reserved_path(hourdate_reserved), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      hourdate_reserved_path(hourdate_reserved),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_hourdate_reserved_path,
            :class => 'btn btn-primary' %>
